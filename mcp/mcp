# !/bin/bash

echo $1
pwd=`pwd`
for i in `ls $pwd/$1 -p | grep -v /`
do
  echo "echo $i 1>&2"
  echo "cat > $i <<'End of $i'"
  cat $pwd/$1/$i
  echo "End of $i"
done
for i in `ls $pwd/$1 -p | grep /`
do
  $0 $1/${i%?}
done
